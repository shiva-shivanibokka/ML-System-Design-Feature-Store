# API + feature store core
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
httpx==0.27.0
duckdb==1.5.4
redis==5.0.4
pandera==0.20.0

# ML + training
lightgbm==4.3.0
scikit-learn==1.4.2
mlflow==2.13.0
# DagsHub tracking works via MLFLOW_TRACKING_URI + username/password env vars —
# no `dagshub` package needed (it pins httpx/fastapi ranges that conflict here).

# Data
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0
pyarrow==15.0.2  # mlflow 2.13 requires pyarrow<16
Faker==25.2.0

# Observability
structlog==24.2.0

# Config
pyyaml==6.0.1
python-dotenv==1.0.1

# Dev / CI
ruff==0.4.8
pytest==8.2.2
pytest-cov==5.0.0
fakeredis==2.23.2
