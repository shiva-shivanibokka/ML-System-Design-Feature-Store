# Backend API base URL. Local: the FastAPI server (default port 8080).
# Production (Vercel): set to the Cloud Run service URL, e.g.
#   https://feature-store-api-XXXXXXXXXXXX.us-central1.run.app
NEXT_PUBLIC_API_URL=http://localhost:8080
