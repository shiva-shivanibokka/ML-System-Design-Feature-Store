[pytest]
# Put the repo root on sys.path so tests can `import feature_store`, `serving`,
# etc. when pytest is invoked as `pytest tests/` (CI) rather than `python -m pytest`
# (which would add the CWD automatically). Makes test collection location-independent.
pythonpath = .
testpaths = tests
